# 更新日志

本文件记录面向使用者的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [0.1.3] - 2026-09-04

### 修复

- **时间轴不再丢掉同毫秒开始的调用**。时间戳只作为横坐标，不再充当调用的唯一键；dsh 并发发出的主调用与 `session-title` 若恰好共用同一个 `startedAt`，两点现在都会保留。此前后一条会覆盖前一条：汇总条明明显示有缓存命中，图表却可能误报「没有调用上报该指标」。按步骤视图仍按步骤合并重试，语义不变。

### 变更

- **台账表格改为单一网格（subgrid），列宽全部由内容推导，表格整体居中**。原先每个 `.rl-row` 是各自独立的 grid，共用一条十三条手工标定的像素轨道（外加一条没有任何单元格的尾部 1fr「停放轨道」）：英文表头 `Cache write`、`Msg/Calls` 的末字母被静默裁掉（实测各缺 5px / 2px），中文 `首字延迟`、`消息/调用` 同样在裁（3px / 2px）；模型列钉死在 280px 上限，装着 41px 的 `glm-5.3`——名字右侧约 240px 空白；1270px 面板下表格右缘内侧还停着 194px 空轨道。现在 `.rl-table` 本身是唯一的网格、每行 `subgrid` 进来，每条轨道的 max-content **跨全部行**测量：列宽＝该列最宽内容（表头或数值），表头在任何语言、字号和文案下都不会被裁。整条手调清单与幽灵轨道删除；表格取内容宽度并在面板中居中，面板窄于内容下限时才横向滚动，不压任何一列。浏览器要求随 subgrid 提升到 Chrome/Edge 117+、Safari 16+、Firefox 71+（均已写入 README 兼容性一节）。
- **Charts 在新会话里默认展开，并可从图点定位台账行**。悬停仍显示精确值；点击或触摸当前吸附点，会把它对应的调用行滚到视口中央并保持选中底色。步骤轴遇到重试时定位到实际提供图中读数的 attempt；时间轴同毫秒有多个调用时再按纵向距离选择真正点中的一个。开关状态仍按会话记忆，用户主动收起后不会被覆盖。
- **主控件回到 dsh 的 12px 正文字号，详情元数据压成一行完整信息层**。Refresh、Copy JSON、Back 等操作原先和表头一样只有 11px，读起来像注释而不是可操作控件；图表内部的高密度切换仍保留 11px。详情页的 Call 卡片现在跨满一行，短字段在卡内自适应分列，不再在右侧留下半行空白，也更早露出请求 / 响应正文。
- **时间列跨天带日期，完整时间戳带时区**。存储的本来就是 Unix 毫秒时间戳，但台账 Time 列只显示 `HH:MM:SS`——一个跨几天的会话里 `16:02:30` 会出现好几次，无从分辨是哪天。现在沿用图表横轴自己的跨天规则：已加载窗口跨过日界，全列以 `09-01 15:14:59` 起头（单日会话保持纯时钟，不添噪音）；图表 tooltip 标题同规则、秒数保留。凡显示完整时刻的地方（悬停提示、详情页「开始时间」）一律追加本地 UTC 偏移（`2026-09-01 15:14:59 +08:00`）——`16:00:04` 只是墙上的钟，偏移量才让它成为一个时刻。
- **详情页 Back 按钮的文本箭头换成内联 SVG chevron**。`'← Back'` 的箭头字形取自正文字体，字重与基线和页面上其他一切对不上，部分 CJK 回退字体里干脆缺字——这正是台账跳转按钮注释里记过的反模式，Back 自己又犯了一遍。chevron 助手挪到 detail 模块共用，补一个 `left` 方向；按钮改为 inline-flex，图标与文字以 5px 间距排列，和汇总条工具栏的两个箭头按钮同一家族。
- **盒状表面的圆角对齐 dsh 的 12px**。dsh 的 boxed 表面（代码块 / diff / 阅读视图 / 搜索 / 终端，`--dsl-*-radius`）清一色 12px，本插件的表格、卡片、代码块、图表面板却各自用 10px——一个两边都不存在的值，正是「说不清哪里不对但就是不像 dsh」的那种来源。四处统一改为 12px；分段控件外壳 6px / 内钮 5px 的嵌套关系不动，那是正确的嵌套圆角。

## [0.1.2] - 2026-09-04

### 新增

- **汇总条工具栏新增「回到顶部 / 跳到最新」两个图标按钮**。长会话的台账钉在最新一条——向上翻回汇总条要滚过几百行，翻上去之后想回最新同样要滚回来。两个按钮并入原有的 Charts / Auto / Refresh 分段控件，箭头是内联 SVG（正文字体的 `↑`/`↓` 字形与周围一切的字重、基线都对不上，部分 CJK 回退字体里干脆缺字）。无处可跳时置灰而非隐藏——滚动时工具栏宽度跟着变，比一个灰掉的按钮更糟。点 ↓ 同时重新钉底，Auto 刷新恢复跟随最新一条。瞬跳不做动画：横跨五万像素的平滑滚动是慢镜头，不是捷径。
  
  按钮**没有**做成悬浮在台账上的圆形浮标：在 Requests 标签页里 dsh 把输入框画在这个面板底部约 130px 之上，右下角的浮标正好被盖住（1000px 高的窗口里实测落在 y=1283，DOM 里在、屏幕上没有）。工具栏骑在置顶的汇总条上，永远可见，也不需要猜宿主在上面画了什么。
- **汇总条新增「失败 / 重试」两格**。此前头条全是会计口径：一个 3 次失败、4 次重试的会话和一个全绿的会话在汇总条上长得一模一样，而请求日志存在的理由首先是「出了什么事」。失败（红）与重试（无色）**只在非零时渲染**——干净会话零噪音，出事的会话躲不过去；失败的悬停拆「N 次错误 · M 次中止」，与台账红点同口径（`error` 与 `aborted`，进行中的不算失败）。这两个数是 dsh 自己的页脚没有的；步骤数与模型用时 dsh 页脚本就带着，不在插件里重复。
- **Token 图表把输出带拆解为「思考 + 回答」**。推理 token 原本只是详情页第七个条件方块，台账、汇总、图表里都没有——而对推理模型，这是最能解释延迟与成本的一个数。现在 token 组的输出段拆成两层：思考 + 回答（回答 = 输出 − 思考），两层之和恒等于上报的输出，堆叠顶、累计柱高仍然是「计费输入 + 输出」——拆解只增加可见性，不增加 token。模型未上报思考时画的是真实的零高度带而非缺口（没思考就是没思考，不是数据缺失）；个别 provider 上报思考数超过输出时钳到输出为止，回答层永不为负。颜色取 dsh 自己的 `state-business-primary` 蓝：它的调色板没有第五个色相，而红色在本 UI 里一律意味着失败。台账 `Out` 列与汇总条仍显示总输出。

### 变更

- **Auto 与 Refresh 各自带上了说明**。两个标签都只说了「自动」「刷新」，没说自动*什么*、刷新出*什么*——`Auto` 最容易被读成「自动跟随最新一行」（那是另一回事，且一直开着：只要你本来就在底部，台账就会跟着走），`Refresh` 则是反复点了才发现「好像没用」（Auto 开着时它确实几乎总在空转）。现在悬停给出：Auto 是每 3 秒轮询新调用，页签切到后台或打开某次调用详情时暂停；Refresh 是立即重载整个已加载窗口，用于刷新失败之后、或 Auto 关掉时重新同步。两个控件的行为一点没改——先前要读代码才知道的事，现在写在了它们自己身上。

- **台账列宽重排：TTFT 与总时长减半，数字列全部定宽，富余宽度归模型列**。原先九个数值列都是 `minmax(56px, 1fr)`，等分剩余宽度后每列约 99px——而 TTFT 与总时长的内容（`185ms`、`1m33s`）是全表最窄的，却和其余列一样宽，导致相邻两个数字之间隔着 50px 死区，一列根本对不齐它旁边那列。现在时间两列各 52px（减半），速度列 74px（要容下 `≈ 0.00 t/s`），两个缓存列 64px（下限由英文表头而非数字决定），模型列吃掉全部富余。窄于约 940px 时表格横向滚动，而不是把任何一列压扁。

- **修好汇总条的置顶：此前它在 dsh 默认布局里根本没生效**。`.rl-root` 声明了 `overflow-y: auto`，因为在宿主给出有界面板时它自己就是滚动容器；但这条声明同时让它在**所有**布局里都成为自己 `position: sticky` 子元素的包含块——包括 dsh 默认布局，那里真正滚动的是祖先面板，`.rl-root` 自己从不滚动。于是汇总条被钉在一个永不移动的滚动视口上，等于没钉：面板滚 600px 时实测它在 y=-508，正是那个 sticky 包装器当初就是为了避免的「读者滚到位、总计却已经滚没了」。现在检测到滚动的是祖先时把 overflow 交还回去，汇总条改为钉在真正滚动的盒子上。顺带修掉表格跳动：那对滚动按钮原先是带真实高度的粘性槽位（一个按钮 30px、两个 66px）再用 `margin-top: -78px` 拉回，净值一个按钮 -48px、两个 -12px，每次跨过显示阈值 flex 列高度都变，表格在光标底下肉眼可见地跳一下并重排列宽。

- **汇总条从三种颜色收回两种**。重试原本是琥珀色：`#f59e0b` 压在 `#fff` 上对比度约 2.1:1，本就读不清，而且紧挨着红色时读起来像第二个警报。七个数字里有三个带色，等于把「命中率是唯一着色项、所以找得到它」这条原则作废了——那句话当时还原封不动留在代码注释里。现在只有失败（红，且仅非零时出现）与命中率（绿）带色，两者语义正好相反。同时在「行计数组」与「token 合计组」之间加了一条细分隔线：`41 8 6 2.77M` 连成一串读不出边界，而用竖线分组正是 dsh 自己状态栏的写法（组间 `|`、组内 `·`）。

- **`formatDuration` 新增小时档（`1h23m`，分钟补零、秒丢弃）**。延迟图的 y 轴刻度走的就是这个函数，超过一小时的时长原先会打印成 `60m0s` 这样的分钟串；取整先算总分钟，`1h59m40s` 不会打印成 `1h60m`。

- **台账汇总条重排：五个头条数字 + 一行具名拆解**。原先是 8 个等重的方块，全部 14px/600——没有主次，眼睛没有落点；而且「总输入」本就等于「输入 + 缓存命中 + 缓存写入」，把和与它自己的加数并排成同级，是把数据关系讲错了。现在头条是调用数、总输入、命中率（唯一着色项，也是最常被扫的数）、输出、新增占用；三项构成收进下方一条以「输入」起头的细线，说明它拆的是谁。数字用 tabular-nums，轮询刷新时不会跳动。字号不是凭空定的：仓内有两处现成参照——dsh 输入框下方那条状态栏（`2 turns · 62 steps | Cache hit 97% | Input 4.6M tok · Output 50.1K`，实测 12px/20px、400 字重、三级灰，组内 `·`、组间 `|`），以及同为插件面板的 dsh-context 统计（`.lc-stat-value` 14px/600 一级标签，压 `.lc-stat-label` 11px/600 二级标签，同样用 tabular-nums）。本条按后者。先前那版把数字放到 17px/600 并加 -0.015em、拆解行放到 11.5px——这三个值在 dsh 与 dsh-context 里一处都不存在，等于从两头同时把值与标签的对比度拉大；真正显得「不是 dsh」的是这个，而不是间距。带单位的值（318.8 KB）不再把单位缩小抬起（那是两边都不玩的排印花招），只靠字重与灰阶区分。拆解行上方的细线也去掉了：它横跨整个台账宽度，去给一句三分之一处就结束的话划底线——dsh 状态栏与 dsh-context 的 `.lc-foot` 都只用空白分隔。信息一个没少，方块全部去掉——`bg-layer-2` 与页面同为 #fff，那 8 个圆角框只画了 8 圈 4% 黑的描边，没有任何对比度；`Output` 原本套的 `brand-text` 也解析成和普通值一样的 #0f1115，同样是无效装饰。整体改用 dsh 自身状态栏的排印语汇（无边框、`·` 分隔、次级灰阶），五个数字与 Charts / Auto / Refresh 同处一行：数字聚成一组靠左，控件锚住右边缘——留白本身不是问题，**没有东西镇住远端**的留白才是；先前用 1fr 把五个数字均摊到整宽，看似填满，实则每两个之间隔着 170px 死区，五个数字不再像一行，而像五个各自漂着的东西。原先的面板标题「Provider calls · N」与「N / 调用数」重复，已去掉，总数改由调用数那一格在分页未载全时以「共 N 次」承载——比原来光秃秃的「100+」还多说明了其余数字是按哪些调用汇总的。整个头部由三行压到两行。

- **所有数值统一为三位有效数字**。`formatDuration`（`5.44s` / `16.3s`）与 `formatTps`（`103` / `66.3` / `1.23 t/s`）本来就各自内联着这条阶梯，而 `formatTokens`、`formatBytes`、`formatPct` 从它漂走了：k 档一位小数、M 档固定两位（`45.00M` 两个废零，`123.46M` 五位有效数字），KB 档一位、MB/GB 档两位（`318.8 KB` 四位，`856.07 MB` 五位），百分比恒定一位（满命中读作 `100.0%`，在一个只有三位的量纲上claim第四位）。现在收敛到同一个 `sig3` 助手，规则不会再各自漂移。同时修掉进位越界：`999_999` tokens 是 999.999k，原样打印成 `1000.0k`；`1024 * 1024 - 1` 字节是 1023.999 KiB，打印成 `1024.0 KB`——两者现在都升一档为 `1.00M` / `1.00 MB`。
- **详情页的「开始时间」不再用 `toLocaleString()`**。原本渲染成 `2026/9/3 16:00:04`，而同一时刻在台账里是 `16:00:04`、在图表横轴上是 `09-03 16:00`——一个瞬间三种写法，彼此无法对读，且随浏览器区域设置漂移。现固定为 `2026-09-03 16:00:04`：同样的分隔符、同样的顺序、同样的本地时区，三者从长到短是同一族。
- **排版对齐到本插件自己的尺度**。调用详情的 `.rl-token-value` / `.rl-token-label` 早就是 14px/600 一级 + 11px/600 二级，反而是汇总条先前跑到 17px——修正后两处一致。另有两个 11.5px（模型名、JSON 正文）归到 12px：本文件的尺度是 14/13/12/11/10，半个像素在 1x 下只会糊成一团。JSON 正文同时改用 dsh 自己的 `--ds-font-family-code`——原先那串 `Consolas, 'Courier New'` **不含任何 CJK 字体**，而这个视图里的提示词正文大量是中文，等于把中文字形丢给浏览器随便挑；dsh 的字体栈里明确带着 `PingFang SC` / `Microsoft YaHei`。字号 12px/1.55 也正是 dsh 代码块与 dsh-context `.lc-ts-arg-val` 的取值。
- **去掉三处不生效的着色**。台账 `Out` 列的表头与单元格、详情页 Output 卡片，都用 `--dsw-alias-brand-text` 声称在强调输出——实测该令牌在**亮暗两套主题下都等于 `label-primary`**（#0f1115 / #f9fafb），也就是和它们本来继承的颜色一模一样，一处都没生效。缓存命中列的绿是真的（`state-success-primary` = #22c55e），输出列不需要再要一个色相。步骤徽标那处 `brand-text` 是真在起作用（把徽标从 `label-tertiary` 提到一级），改写成它实际解析到的 `label-primary`，名副其实。

- **分段控件的选中态改为浅底，不再是纯黑实心**。`Charts` / `Auto`、调用详情的请求/响应与格式切换、图表的指标切换，选中时原先填 `--dsw-alias-button-primary-fill`——该令牌在亮色主题下解析为 #0f1115，于是两颗近黑药丸成了整个视口里最重的墨，比标签栏、表头和图表都重。dsh 核心一处都不这么填：它的选中标签页是品牌蓝**文字**，它的按钮（`Session log`）是白底描边。现改用 `--dsw-alias-interactive-bg-hover-solid` 作底，选中感由一级标签色加 600 字重承担，暗色主题下该令牌自动换成浅灰。分段容器的边框同时从 `border-l1`（4% 黑，白底上根本看不见，选中块像浮着没有依附）换成 `border-l2`——正是 dsh 自己按钮所用的那个 `1px rgba(0,0,0,0.1)`。

- **图表横坐标可切换时间 / 步骤，默认按时间**。此前只有编号轴（#1、#2……），每个回合等宽——密集连发和中间空了一小时看起来一模一样。现在默认把每次调用放在它真实发生的时刻：连发会挤成一簇，空闲就是一段看得见的空白，一眼能看出**你什么时候请求了什么**。刻度对齐本地整点/整日（跨天后标签带日期）；时间轴上每次重试各自成点（在时间里重试就是又一次请求），压缩与起标题等辅助调用也一并画出——命中率的悬崖往往正是被压缩解释的，此时 ⓘ 排除徽标自然消失。累计模式在时间轴上改画堆叠**面积图**：柱宽取自相邻间距，在连发处会缩成 1px 的碎片。切回**按步骤**即恢复原本的编号轴与堆叠柱，选择按会话记忆。

- **台账汇总条显示本会话的「新增占用」**。读 API 的列表响应新增 `storage` 字段（信封字节、物化对象字节、两者之和，以及当前单会话上限），汇总条末尾多一格显示合计，悬停给出拆分与占上限的比例。这个数就是 `maxFileBytes` 计费的那个数，所以看到的和被裁剪时依据的是同一套账。**它是增量而非对话内容的重量**：已落盘的片段不计费，重试记 0，共享片段记在先写入的一方，因此各会话的数字加起来不等于存储目录的大小（pack 索引、块条目表等公共开销不归属任何会话）——文案统一说「新增」，不说「总大小」。统计随信封行的增量解析累加，轮询不会为此多读一次文件。

## [0.1.1] - 2026-09-01

### 修复

- **图表悬停/点击吸附到最近的数据点**。此前指针的像素横坐标被拿来和数据坐标（步骤号）比大小，悬停几乎永远落在最后一个点上；现在按绘制像素位置吸附最近的数据点，超过 200 点抽稀后的悬浮取值也对齐全分辨率数据，点击/触屏点按同样吸附。
- **输出速度三处统一口径**（台账行、调用详情、图表）。同一读数：流阶段可测且速率可信时为精确值；流阶段过短（不足 200ms 的一次性返回）或精确速率超出可信上限（500 t/s，粗粒度分包）时，回退为「输出 ÷ 整次调用」并标 `≈`、悬停说明——台账行原先在此显示 `–`，与图表的 `≈` 值互相矛盾。对一次性返回的响应，等待即生成，整通速率是诚实的估计而非编造的精度。

## [0.1.0] - 2026-08-31

首个发布：一个记录 dsh 每次 Provider 请求与响应的插件——发出去的原文、收回来的全文。

### 新增

- **每一次真实调用都被完整记录**。透明挂在 `llm/stream` 瀑布线上，捕获后原样放行：系统提示、消息、工具 schema、采样参数，以及组装完成的响应——内容块、usage、finish reason、计时。失败调用的每次重试也各自成行，按请求哈希关联并标注 `attempt: N`；普通调用带 `#N` 步骤号徽标（会话第几步调用的），压缩和起标题等辅助调用标注 `purpose` 且不占步骤轴。
- **一次调用、四种视图**。Neutral 是跨过 `llm/stream` 边界的原始捕获；OpenAI ChatCompletion（`reasoning_content`、`role: 'tool'` 行、`cached_tokens`）、Anthropic Messages（`system` 块、`tool_use`/`tool_result`、`cache_read_input_tokens`）、OpenAI Responses（`store: false`、完整 `input`、原生 `reasoning: { effort, summary }`）三个 wire 视图从同一份捕获忠实重建——视图之间的差异就是协议之间的差异。适配器无从得知的细节（Anthropic 思考模式、思考签名、适配器默认 `max_tokens`）以合法 wire 形状呈现并用命名空间化的 `_note` 标注，绝不编造值；上一调用已知时，用标注和横幅重建有状态客户端*本可以*发送的 `previous_response_id`，并明确标记为重建。调用按其 provider 与模型在对应视图打开（Claude 路由开 Anthropic，Codex 型开 Responses），选择按会话记忆。
- **JSON 渲染为可折叠树**。容器默认折过第 2 层，长字符串（一份 40KB 的系统提示）收在 `… +N chars` 开关后，Expand / Collapse 按钮控制整个视图。
- **用量统计折线图**。台账上方的可开关图表面板：每步缓存命中率（固定 0–100 % 轴，冷启动、压缩后回落、出错断档一眼可见）、token 量（in / cache-hit / cache-write / out；默认开启的**累计**模式按 Cursor 用量仪表盘的样式渲染为运行总量的堆叠柱，每步一列，错误步把总量前推而不是砸出缺口，悬停显示各序列累计与本步增量，图例可整列剔除；关闭后回到每步折线并可按步堆叠）、延迟（总耗时 vs TTFT）与出速（流阶段 token/s）。悬停出精确值；出错/中止的调用画断线而不是假零；辅助调用不上轴（ⓘ 徽标计数）。图表开关状态与当前指标随会话记忆。
- **台账细节**。时间顺序、最新在底（与 Trajectory 一致）：状态点、模型、步骤徽标、finish reason、TTFB / 总时延、token 数、消息/工具列——工具数字数的是**调用次数**（响应发出的工具调用块，`run_code` 程序体内的 `tools.x(...)` 派发点即为传输调用，悬停看分工具明细）。先载入最新 100 条，**Load older** 分页前翻且滚动位置锚定；重试显示 `×N` 徽标。**Auto** 在会话运行时每 3s 自动刷新（页签隐藏时暂停）；瞬时刷新失败保留已载入数据并显示过期横幅，而不是清空台账。
- **你的位置被记住**。页签切走、乃至刷新页面之后，每个会话重开你正查看的那次调用、其请求/响应侧与格式、分页窗口和 Auto 开关——会话各记各的。
- **持久化与保留**。每会话一个 JSONL 文件，落在 `$DSH_HOME/request-log/`：保留期默认 14 天（`never` 永久保留，跟随 dsh 自身会话日志的做法），每会话调用上限默认 2000、字节上限默认 128 MiB——按 append **实际物化**的压缩内容计费：已落盘的片段不重复计费，一次重试计 0，两个会话共享的片段记在先写它的一方——超限从最旧记录开始裁剪。
- **v3 信封让每行保持扁平**。每行携带整棵请求片段树的哈希、响应体哈希与本次物化的压缩字节数 `zn`；正文与树存放在 git 风格的内容寻址对象库（deflate 压缩）。树是*关键帧*，其后每轮只写增量（父指针链，每 32 节点重新关键帧化）——真实 2570 行的库实测：中位数 774 B、最小 656 B、最大 1147 B，会话再长信封也不涨。v1 旧行每行重列全部消息哈希、随历史线性增长（占真实库信封字节的 97.6 %）；旧文件按需惰性迁移。读路径同样便宜：列表分页只投影信封，详情才解析树（有界父链回溯）并重组正文。
- **对象打包（pack）**。每日清扫把冷的可达对象搬进 `objects/packs/` 下的固态块 pack 文件——打包等待 GC 宽限下限，进行中的 append 永远不会丢正文；`pack: off` 是双向门：停打包并逐步把 pack 解回松散对象，可安全降级到读不了 pack 的旧版本。松散期磁盘占用约为压缩内容的 2×（97 % 的对象 ≤ 4 KiB、各占一个文件系统簇；实测 11,279 个对象存 15.34 MB、松散占 30.86 MB），清扫打包后回到约 1×。
- **fail-soft**。捕获与存储的失败绝不打断模型调用；半写的行在下一次 append 时修复；读 API 是可选的（无头组合直接跳过）。
- **带篱笆的读 API**。同源 GET 路由 `/dsh-request-log/*` 只服务环回主机加你配置的 `trustedHosts`；DNS 重绑定与跨站请求在任何读取之前被拒；名为环回的 `Host` 头只有在连接本身是环回时才被采信——局域网对端伪造不了。
- **隐私**。常见凭据形状在捕获时打码；图片只记附件引用、永不存字节。README 的 Privacy 一节写明明文落盘的全部位置与彻底清除的方法：删一个会话的 `.jsonl` **不**等于删掉它的内容——对象要到清扫 GC 才回收，插件卸载后清扫不再运行，彻底清除请删整个 `request-log/` 目录。

### 内部

- CI 在 ubuntu + windows × Node 22 / 24 矩阵上跑 lint / typecheck / 全量测试 / 构建 / tarball 清单检查；另有 engines-floor 任务在 Node 20 上对两个产物 `node --check`，验证 `engines.node >= 20` 的语法声明（es2023 目标）。
- 基于 `@deepseek-ai/dsh` 0.1.1-rc.2 的 wire 类型构建（`@deepseek-ai/dsh-llm` 0.1.1-rc.2）。
